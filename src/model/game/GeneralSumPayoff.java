/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package model.game;

/**
 *
 * @author drew
 */
public class GeneralSumPayoff implements Payoff{

    /**
     * Initialize the matrix with Payoff
     * this only works for two agents.  However,
     * the GameMatrix can work for n number of agents.
     * @param matrix 
     */
    @Override
    public void initPayoffMatrix(GameMatrix matrix) {
        // The values in the array are PayoffValues so it is a payoff[]
        //matrix.getArray();
        //for (int i = 0; i < vals.length; i++)
        {
           // vals[i] = new PayoffValue();
        }
        
        
    }
    
}
